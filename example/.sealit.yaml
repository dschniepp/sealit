sealing_rules:
  - file_regex: \.dev\.yaml$
    name: mysecret
    namespace: default
    encrypt_regex: (password|pin)$
    maxAge: 720h
    cert: 
      path: cert.pem
  - file_regex: \.prod\.yaml$
    name: mysecret
    namespace: default
    encrypt_regex: (password|pin)$
    maxAge: 720h
    cert: 
      path: cert.pem